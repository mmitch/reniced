# this is the configuration file for the integration tests
# set all foo commands to nice level 5
5	^foo
# set long-pid command to OOM adjustment -10 and nice level 3
o-10n3	long-pid
# set bar without parameters to IO class idle
i	bar$
